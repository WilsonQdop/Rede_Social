spring:
  application:
    name: redesocial

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:redesocial_db}
    username: ${DB_USER:Qdop}
    password: ${DB_PASS:Qdop123}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect